# SAMA -- pinned to what is verified working on the build machine.
# Install torch FIRST from the PyTorch index, it is not on PyPI with CUDA:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

ultralytics>=8.4.0          # YOLO26 checkpoints; older releases have no yolo26*.pt
opencv-contrib-python>=4.7  # contrib, NOT opencv-python -- we need cv2.aruco
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9     # FastAPI multipart uploads
pillow>=10.0
numpy>=1.26
scipy>=1.11
matplotlib>=3.8
pandas>=2.0
qrcode>=7.4                 # certificate QR -> public verify page (optional at runtime)
pytest>=8.0
