# DECISIONS

| # | Date | Decision | Rationale | Evidence |
|---|------|----------|-----------|----------|
| D1 | 2026-08-24 | Model locked to openrouter/stealth/ox-alpha for all agents | explicit user requirement; model present in opencode catalog | `opencode models` output |
| D2 | 2026-08-24 | Ship end2end=False (NMS path) | +0.5 mAP, refer rate 1.4% vs 14.1% | STATUS.md measured table |
| D3 | 2026-08-24 | mat_edge rung demoted below carried, confidence 0.25 | 16.9mm MAE vs caliper truth | occlusion_stress + caliper per-rung table |
| D4 | 2026-08-24 | merge_looks uses max per-look defect rate, pooled sizes | concatenation preserves occlusion bias | Defect MAE 7.34→2.95 |
| D5 | 2026-08-24 | ICAR-DOGR band attribution FLAGGED, not changed | research found published DOGR sources use A >60mm/B 50-60/C 35-50, not A >80. CLAUDE.md mandates >80 bands; changing frozen grading math + all UI on one literature pass is risky. Action: verify with the team's official problem-statement docs; if confirmed wrong, unfreeze grading.py with tests in Phase C | .agent/EVIDENCE_AUDIT.md |
| D6 | 2026-08-24 | PS number 26046 vs SIH26031 flagged for confirmation | archive search suggests DoCA onion PS is SIH26031 | .agent/EVIDENCE_AUDIT.md |
| D7 | 2026-08-24 | D5 RESOLVED - keep A>80 bands + ICAR-DOGR name. DS-001 web verification found dogr.res.in Agropedia source supporting >80; R-001's counter-sources were grader sieve classes, not grade standards. Mitigation: cite source+access date on report.html method note; correction appended to EVIDENCE_AUDIT.md | DS-001 report, HIGH confidence |
| D8 | 2026-08-24 | RT-001 S-3 mitigated WITHOUT unfreezing grading.py: defect-rate Wilson interval computed as an overlay in arbitration.defect_rate_interval/defect_ci_fields and merged into result_json at /finalize. Reconstructs (k,n) of the worst look from merge_looks' own published per-look rates so it cannot drift from the printed statistic; degenerate captures get no interval rather than an invented one. Interval inherits S-1 caveats and says so on the certificate ("least-occluded view"). Unfreezing merge_looks (S-1/S-2) remains a data-scientist decision | tests/test_arbitration.py, tests/test_finalize_defect_ci.py; live smoke lot defect_ci_low=24.19 |
| D9 | 2026-08-24 | Capture-quality gate added (app/capture_quality.py): frames with mean luma <40 or >246 are refused at /analyze with an actionable 422 BEFORE inference (untested field condition: godown-after-dusk / flash-blown trays silently producing detections on noise); dim (<70) and blurry (Laplacian var <20 at fixed 480px sampling width) warn without blocking. Thresholds deliberately conservative; replay mode unaffected (client-side path). Audit markings also applied to judge-facing artifacts: prototype label on certificate masthead (§3.3), internal-rot blindness sentence in method note (§3.1), DEMO DATA chip on dashboard (§3.4) | tests/test_capture_quality.py; scripts/smoke_test.py ALL PASS after change; bench GATE T12 PASS |
| D10 | 2026-08-24 | Client-side downscale before upload locked to long edge ≤1600 px + JPEG q80 (index.html UPLOAD_MAX_EDGE/UPLOAD_JPEG_QUALITY): raw 12MP frames (3–8 MB) made a lot cost 40–120 s per look on ~0.5 Mbps rural 3G — the single largest wall-clock+data cost in the product. Model letterboxes to 1024 px internally so nothing detection-relevant is lost; server bbox/annotated_scale math derives from received pixels so coordinates stay consistent. Honesty guard ships the ORIGINAL whenever the re-encode is not smaller, UI shows before→after KB per look + verified session savings (incl. "sent as-is"), bilingual EN/HI. Simulated 12MP noisy frame: 5925 KB → 187 KB/look (97% less), 95 s → 3 s @0.5 Mbps; 6-look session 34.7 MB → 1.09 MB (<2 MB budget). Runtime UI numbers are the ground truth per device; simulation labelled as such | tests/test_upload_downscale.py (15 contract tests); node --check inline JS OK; 166 passed |
| D11 | 2026-08-25 | RT-001 S-2 mitigated WITHOUT unfreezing grading.py, extending the D8 overlay pattern: arbitration.grade_a_ci_fields(result) runs at /finalize BEFORE db.insert_lot, recomputing the Grade-A Wilson interval at effective sample size n_eff = n_obs/deff with Kish deff = 1+(n_looks−1)·ρ and worst-case ρ=1 (a repeat look credits each physical bulb once — never claims more distinct evidence than single-look analysis). Both counts scale by 1/deff so p_hat is preserved exactly: the interval cannot drift from the printed statistic. Frozen pooled bounds survive as grade_a_ci_pooled_low/high for provenance; single-look lots reproduce the frozen width bit-for-bit and label wilson-independent; legacy hash-frozen certificates are NOT rewritten (row_hash covers ci_low/ci_high) and self-label by absence of the new keys. UI prints "≈N distinct bulbs" on capture + report screens only when keys exist. Defect CI needed no change (D8 already uses the worst single look = the ρ=1 answer). Follow-ups flagged, not done: twin.py _saleable_block pooled-n Wilson (S-2a), sample_sufficiency plans in observations not bulbs (S-2b), ρ measurable later via cross-look IoU matching (STATISTICS_NOTES §7) | tests/test_two_look_ci.py (17 contract tests incl. hand-computed Wilson anchors); full suite 232 passed; derivation + scope limits in .agent/STATISTICS_NOTES.md |
| D12 | 2026-08-25 | RT-001 T-6 fixed frontend-only (backend contract already correct): verify.html banner now keys off lot.this_record_ok ALONE; centre-wide chain.intact is demoted to a separate amber context card ("another record was altered — this record verifies intact") so tampering one lot can no longer falsely redden its neighbours. Adopts EVIDENCE_AUDIT V1 wording "RECORD INTACT" with an explicit integrity≠accuracy disclaimer. Copy aligned to audit_chain's real non-cascading behaviour on report.html + db.py docstring (doc-only); unsupported "signed on-device by both parties" claim replaced with taps-not-a-cryptographic-signature wording (T-2/P4 partial). No db.py/API/grading.py change — audit_chain already pinned breaks per-record | tests/test_verify_scoping.py (10 tests incl. 3-lot tamper isolation via API); live page render asserted for intact lot in broken centre |
| D13 | 2026-08-25 | Session checkpoint against tab discard/reload mid-lot (untested field condition: low-RAM Android discards background tabs; officer returned to a wiped form). index.html saves sama_session_v1 to localStorage AFTER each successful capture (never before, replay never); quota-exceeded degrades to metadata-only with images_dropped=true honestly flagged; storage failure never blocks capture; finalize clears it; restore prefills via .value/.textContent only (innerHTML banned in that block), validates restored centre still exists, rebuilds shots from looks so inspect+finalize work unchanged | tests/test_session_resume.py (14 static contracts); node --check on extracted inline JS rc=0 |
| D14 | 2026-08-25 | LOOP-D2262: S-2 follow-ups closed WITHOUT unfreezing grading.py, reusing the D11 worst-case design effect (Kish deff=1+(m−1)·ρ, ρ=1) in the two consumers D11 flagged. S-2a twin._saleable_block: saleable Grade-A interval now arbitration.wilson_pct_effective(count, n, deff) with n_looks threaded from result_json through both simulators; adds n_effective/design_effect/ci_method to each block — the twin card can no longer promise ±12 pts beside a certificate carrying ±16.5 on the same data. S-2b sample_sufficiency(n_obs, …, n_looks=1): current precision evaluated at n_eff (same arithmetic as grade_a_ci_fields, so card ⊇ certificate width), requirements quoted in BOTH units — n_required_distinct_bulbs (design-invariant z²p̂q̂/w²) and n_required_observations (= distinct × deff); at ρ=1 advice now says more shakes of one tray add little, add trays. /api/sufficiency passes stored n_looks; index.html sufficiency chip shows "≈N distinct bulbs" bilingually when deff>1. Clamp rule everywhere: n_looks∈[1,n_obs], garbage→1 look (never invents a multi-look design). Scenario intervals carry the same deff by construction — per-bulb path draws independent degradation uniforms per ROW, so simulated duplicates correlate LESS than real repeats: applying deff there is conservative, never anti-conservative | tests/test_two_look_followups.py (22 contracts incl. D11 anchors Wilson(36,60)/Wilson(18,30)/Wilson(21,35)); full suite 434 passed; derivation + scenario-correlation note in .agent/STATISTICS_NOTES.md §8 |
| D15 | 2026-08-25 | LOOP-I858: RT-001 S-1 (CRITICAL, max-per-look defect estimator) answered by VISIBLE COMPENSATION without unfreezing grading.py — arbitration.tray_pooled_defect_fields(result, looks) runs at /finalize after grade_a_ci_fields, publishing defect_pooled_* (plain pooled incidence + per-tray clustered Wilson interval at n_eff = Σ_t n_t/m_t, worst-case ρ=1 within tray, full credit across trays; single look reproduces plain Wilson bit-for-bit). Counts rebuilt ONLY from published class_counts (D8 discipline). report.html defect card prints ceiling vs cross-check with an explicit errs-high sentence; index.html result screen shows it bilingually; legacy certificates render unchanged (presence-gated keys). Unfreeze decision now has a measured cost of delay: IMPACT_EVIDENCE §3d puts the structural max() bias at ≈+17 pts at the fitted design, ~7× the ±6-pt sampling promise by five trays (lower bound, perfect-detection assumption) | tests/test_defect_crosscheck.py (17), tests/test_i858_ui_contracts.py (10); STATISTICS_NOTES.md §9 |
| D16 | 2026-08-25 | LOOP-I858 edge-case hunt: HEIC/HEIF/AVIF uploads (iPhone default camera format) are refused at /analyze BEFORE decode with an actionable message naming the format and both fixes (iOS Most-Compatible setting / export as JPEG) plus an explicit retry-will-fail warning. Previously cv2.imdecode returned None → generic "Could not read that image. Try again." advice that could never succeed on a retake. ISO-BMFF ftyp sniff (~50 byte comparisons) checks major + compatible brands; ordinary brands pass through untouched | tests/test_heic_guard.py (9 incl. JPEG-passthrough); sniff placed before aruco/model guards so the refusal is deterministic regardless of model presence |
| D17 | 2026-08-25 | LOOP-I858 INCIDENT FIX: /tamper/restore-all's fallback sweep trusted result_json as restore-truth for EVERY row — but legacy v1 rows (result_sha256 NULL) never signed that blob, and a v2 row whose digest mismatches was edited by its attacker. Live consequence during this loop's demo drill: legacy lot 51 (seeded Chennai-11) had pre-envelope damage (99.9 inside the blob); sweep wrote it into the signed grade_a_pct column and broke centre 2's chain. Signed value recovered by brute-forcing the pinned row_hash over the 2-dp grid (4.05), data repaired byte-exactly. Fix: sweep 2 uses result_json ONLY when result_sha256 exists AND db.result_digest matches; everything else is skipped and NAMED in the response (skipped_unverifiable). tamper-demo.html states why records were left as-is. Double-tampered rows stay flagged for a human rather than being silently rewritten from attacker bytes | tests/test_restore_all_legacy_guard.py (4 contracts incl. faithful v1 degradation via recomputed summary-only hash); drill re-run on fixed server: intact_after=True across all 7 centres |
